name: Build and publish JAR

on:
  push:
    branches:
      - '*'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: Set up JDK 8
        uses: actions/setup-java@v2
        with:
          java-version: '8'
          distribution: 'adopt'
            #- name: Build with Gradle
            #run: ./gradlew conductor-server:shadowJar --stacktrace
      - name: Build artifact
        run: mkdir -p server/build/libs && echo "test" > server/build/libs/conductor-server-test-all.jar
      - name: Create build tag
        run: |
          echo "::set-output name=TAG::$(git describe --abbrev=0 --tags)-$${{ github.run_number }}+${{ github.sha }}"
        id: tag
      - name: Upload conductor-server JAR
        uses: ncipollo/release-action@v1
        with:
          artifacts: "server/build/libs/conductor-server-*-all.jar"
          commit: "${{ github.sha }}"
          tag: ${{ steps.tag.outputs.TAG }}
          token: ${{ github.token }}

